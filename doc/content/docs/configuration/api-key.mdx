---
title: API Key 配置
description: 在扩展内选择 AI 服务商、填写模型与 API Key 的完整指引
---

import { Callout } from 'fumadocs-ui/components/callout';
import { Steps, Step } from 'fumadocs-ui/components/steps';

扩展的 AI 能力基于 **OpenAI Chat Completion 兼容协议**，并提供 **通义千问 / Kimi / OpenAI / GLM / 自定义** 五种配置入口。v1.2.18 起仅使用用户自行配置的 API，不再提供内置免费 AI / AIGate。

## 配置方式

在 **选项页 → 配置** Tab 中填写你自己的 API Key、模型和 Base URL。AI 请求只会按这里的自定义配置发送。

## 自定义配置（推荐）

在「自定义配置」模式下，表单包含以下字段：

| 字段 | 必填 | 说明 |
|------|------|------|
| **AI 模型** | ✅ | 服务商适配器（通义千问 / Kimi / OpenAI / GLM / 自定义） |
| **模型** | ✅ | 具体模型名称，如 `deepseek-chat`、`qwen-plus`、`moonshot-v1-8k` |
| **API Key** | ✅ | 从服务商后台生成的密钥，输入框支持密文显示 |
| **Base URL** | ⚪ | 可选，留空时使用所选服务商的官方地址 |
| **Extra Params** | ⚪ | 仅在选择「自定义」适配器时可编辑的额外 JSON 参数 |

<Callout type="info">
  字段修改会 **自动保存**（表单 `onChange` 即触发写入），无需点击额外的保存按钮。
</Callout>

### 服务商内置默认地址

当你在下拉中选择某个服务商后，Base URL 会自动填入官方默认值：

| 适配器 | 默认 Base URL |
|--------|---------------|
| 通义千问（qianwen） | `https://dashscope.aliyuncs.com/compatible-mode/v1` |
| Kimi | `https://api.moonshot.ai/v1` |
| OpenAI | `https://api.openai.com/v1` |
| GLM | `https://open.bigmodel.cn/api/paas/v4` |
| 自定义（custom） | （留空，由你填写） |

<Callout type="info">
  固定 `host_permissions` 只保留 Bilibili、OpenAI 与讯飞域名；其他自定义地址使用扩展声明的可选站点权限。若请求被浏览器拦截，请在扩展详情页检查对应站点访问权限。
</Callout>

### Extra Params 字段

- 选择 **内置适配器** 时此字段为只读，内置值会用于关闭「思考模式」等优化；
- 选择 **自定义** 适配器时可写入合法的 JSON 字符串，例如：

```json
{
  "chat_template_kwargs": { "enable_thinking": false }
}
```

## 完整配置流程

<Steps>
  <Step>
    ### 打开配置页
    右键扩展图标 → **「选项」**，或在弹窗中点击齿轮图标 → 进入 **「配置」** Tab。
  </Step>
  <Step>
    ### 选择 AI 模型
    在 **「AI 模型」** 下拉中选择对应服务商。选择后 Base URL 会自动填充。
  </Step>
  <Step>
    ### 填写模型名称
    在 **「模型」** 输入框中填入服务商支持的具体模型 ID，例如：

    - 通义千问：`qwen-plus` / `qwen-turbo`
    - Kimi：`moonshot-v1-8k` / `moonshot-v1-32k`
    - OpenAI：`gpt-4o-mini` / `gpt-4o`
    - GLM：`glm-4-flash` / `glm-4-plus`
    - 自定义：按你的代理后端配置填写
  </Step>
  <Step>
    ### 填写 API Key
    点击 API Key 输入框右侧的 👁 图标可在密文/明文之间切换，方便核对。
  </Step>
  <Step>
    ### （可选）调整 Base URL
    如果你使用自部署的代理或非默认区域的端点，可在 **「Base URL」** 中覆写。
  </Step>
</Steps>

## v1.2.18 的 AI 路由

- 只使用当前配置页保存的自定义 AI；
- 不再连接 AIGate / `radiumg.top`；
- 旧版本若保存过 `free` 模式，升级后会自动迁移回 `custom`，但不会伪造或自动填入 API Key。

## 相关文档

- [模型选择与推荐](/docs/configuration/model-selection) — 如何选对模型
- [AI 智能分类](/docs/guide/ai-classification) — 模型在扩展中的使用方式
